'use strict';

let hasDriversLicence = false;
const passTest = true;
if (passTest) hasDriverLicence = true; // strict mode will tell this bug
if (hasDriversLicence) console.log('I have a driving licene') 